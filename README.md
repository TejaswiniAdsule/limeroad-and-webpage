 Limeroad Web Page Clone

A static front-end clone of the Limeroad web page built using HTML and CSS. The project focuses on creating a clean, structured layout by heavily leveraging the CSS `display: flex` property to align components, manage spacing, and replicate the original fashion e-commerce design aesthetic.
